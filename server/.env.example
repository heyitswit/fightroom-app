# Port to listen on (Node/Bun)
PORT=3000

# Where the JSON database is stored (Node/Bun only). Defaults to ./data/db.json
# DATA_FILE=./data/db.json

# Set to "production" to disable dev mode / verbose errors
# NODE_ENV=production
